
# Citrino - Relatório de Performance Financeira 📈

Este projeto é um protótipo de dashboard financeiro desenvolvido em **React** para a visualização de dados de performance de ativos e fundos. O objetivo principal foi criar uma interface de alta fidelidade que simula o comportamento de um sistema real, onde o conteúdo das tabelas é recebido dinamicamente via backend.

## 🚀 Tecnologias Utilizadas

* **React 18** com **TypeScript**
* **Vite** (Build tool)
* **Tailwind CSS v4** (Estilização baseada em utilitários)
* **React Icons** (Font Awesome & BoxIcons)
* **Google Fonts** (Comfortaa, Fredoka, Karla)

---

## 🛠️ Como Executar o Projeto

Para rodar este projeto localmente, você precisará ter o **Node.js** instalado.

### Pré-requisitos
* **npm** (instalado automaticamente com o Node.js)

### Passo a Passo

1.  **Clonar o repositório:**
    ```bash
    git clone [https://github.com/amorimbreno/demo-tabela.git](https://github.com/amorimbreno/demo-tabela.git)
    ```

2.  **Entrar no diretório do projeto:**
    ```bash
    cd demo-tabela
    ```

3.  **Instalar as dependências:**
    ```bash
    npm install
    ```

4.  **Iniciar o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```

Após o início, o terminal exibirá o endereço local (geralmente `http://localhost:5173`). Abra-o em seu navegador para visualizar o dashboard.

Desenvolvido para o processo seletivo da **Citrino**.
